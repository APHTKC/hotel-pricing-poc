name: Probe Grand Mayfull Taipei adapter

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scrapers/adapters/grand_mayfull.py"
      - "scrapers/registry.py"
      - "scripts/run_grand_mayfull_once.py"
      - "config/hotels.grand-mayfull-taipei-only.yaml"
      - ".github/workflows/probe-grand-mayfull-taipei-adapter.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium
      - name: Collect one public stay date
        env:
          DEMO_MODE: "false"
          STORAGE_BACKEND: local
          LOCAL_DATA_PATH: data/grand-mayfull-taipei-probe.jsonl
        run: python -m scripts.run_grand_mayfull_once
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: grand-mayfull-taipei-adapter-probe
          path: data/grand-mayfull-taipei-probe.jsonl
          if-no-files-found: ignore
